baseURL = "https://alsspp01.github.io"
defaultContentLanguage = "en"
languageCode = "en"

title = "Dyun's blog"

theme = "gokarna"

# Automatically generate robots.txt
enableRobotsTXT = true

[menu]
  [[menu.main]]
    # Unique identifier for a menu item
    identifier = "posts"

    url = "/posts/"
    
    # You can add extra information before the name (HTML format is supported), such as icons
    pre = "<span data-feather='pen-tool'></span>"


    # You can add extra information after the name (HTML format is supported), such as icons
    post = ""

    # Display name
    name = "posts"

    # Weights are used to determine the ordering
    weight = 1

  [[menu.main]]
    identifier = "projects"
    name = "projects"
    url = "/projects/"
    weight = 2

    pre = "<span data-feather='box'></span>"

  [[menu.main]]
    identifier = "tags"
    name = "tags"
    url = "/tags/"
    weight = 3

    pre = "<span data-feather='hash'></span>"

  [[menu.main]]
    identifier = "github"
    url = "https://github.com/alsspp01"
    weight = 4
    
    # We use feather-icons: https://feathericons.com/
    pre = "<span data-feather='github'></span>"

[params]
  # favicon
  favicon = "/image/profile/favicon-16x16.png"
  
  # URL for the avatar on homepage
  avatarURL = "/image/profile/MainCat.jpg"

  # Choose one of size-xs, size-s, size-m, size-l & size-xl. (Default: size-m)
  avatarSize = ""

  # Description to display on homepage
  description = "Republic of Korea 대한민국"

  # Accent color is displayed when you hover over <a> tags
  accentColor = "#4d93fd"

  # Display "back to top" button on posts
  ShowBackToTopButton = true

  # You can use this to inject any HTML in the <head> tag.
  # Ideal usecase for this is to import custom js/css or add your analytics snippet
  customHeadHTML = ""

  socialIcons = [
    {name = "instagram", url = "https://www.instagram.com/dyun15min?igsh=OGx4Y2l5YzN4aDll"},
    {name = "email", url = "mailto:alsspp01@gmail.com"}
  ]
  
  # Keywords relevant for SEO
  metaKeywords = ["blog", "gokarna", "hugo"]

  # If you want to display posts on the homepage, the options are
  # "popular" (order posts by weight), "recent" (order posts by date)
  # or "" (do not display, default option)
  showPostsOnHomePage = "popular"

  # Footer text
  footer = "The Marauders"
  
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

[security]
  [security.funcs]
    getenv = ["^HUGO_", "^WC_"]
